---
name: 2.9 Image
menu: 2. Components
---

import { Playground, PropsTable } from 'docz';
import { Image } from '../../src';

# Image

It use `<img>` tag. With a default style: `max-width: 100%; height: auto;`

<Playground>
  <Image />
</Playground>

## Properties

All `props` from `<BaseBox>` and `<Input>`.
