---
name: 2.3 FlatButton
menu: 2. Components
---

import { Playground, PropsTable } from 'docz';
import { FlatButton } from '../../src';

# FlatButton

It use `<a>` tag.

<Playground>
  <FlatButton>OK</FlatButton>
</Playground>

## Properties

All `props` from `<BaseBox>`. Along with a `disabled`
