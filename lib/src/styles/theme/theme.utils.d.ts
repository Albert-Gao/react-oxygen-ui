import { ITheme } from './theme';
export declare const createTheme: ({ breakpoints, fontSpecs, fontSizes, space, colors, fontWeights, borders, radii, shadows, gradients, }: Partial<ITheme>) => ITheme;
